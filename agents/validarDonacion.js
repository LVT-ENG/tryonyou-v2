const validarDonacion = {
  descripcion: `Verifica si una prenda cumple los requisitos para ser donada de forma automática.`,
  ejemplo: `validarDonacion({ estado: 'bueno', valorEmocional: false }) // -> true`
};

module.exports = validarDonacion;
