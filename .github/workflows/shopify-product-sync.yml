name: Shopify Product Sync

on:
  push:
    paths:
      - 'shopify/products.csv'

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trigger Make Scenario
        run: curl -X POST "${{ secrets.MAKE_WEBHOOK_URL }}"
