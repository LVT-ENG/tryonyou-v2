const compararTalla = {
  descripcion: `Compara las medidas del usuario con la base de datos de prendas y devuelve la talla óptima.`,
  ejemplo: `compararTalla({ pecho: 90, cintura: 70 }) // -> 'M'`
};

module.exports = compararTalla;
